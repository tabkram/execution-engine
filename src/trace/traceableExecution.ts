import { TraceableEngine } from '../engine/traceableEngine';

/**
 * @deprecated Use `TraceableEngine` instead.
 *
 * ⚠️ `TraceableExecution` is deprecated and will be removed in a future release.
 * Migrate to `TraceableEngine` to ensure compatibility with future updates.
 */
export class TraceableExecution extends TraceableEngine {}
